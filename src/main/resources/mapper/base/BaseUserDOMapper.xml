<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.caker.mall.dal.base.mapper.BaseUserDOMapper">

    <sql id="BASE_COLUMN">
        id, gmt_create, gmt_modify, creator, modifier,
        is_deleted, uid, user_name, nick_name,
        email, avatar, phone, status, sex, birthday,
        signature, location, address, remark
    </sql>

    <insert id="insert" parameterType="cc.caker.mall.dal.base.po.BaseUserDO">
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            select last_insert_id()
        </selectKey>
        insert into base_user
        (gmt_create, gmt_modify, creator, modifier,
        is_deleted, uid, user_name, nick_name, password,
        email, avatar, phone, status, sex, birthday,
        signature, location, address, remark)
        values (sysdate(), sysdate(), #{creator}, #{modifier}, 'N',
        #{uid}, #{userName}, #{nickName}, #{password},
        #{email}, #{avatar}, #{phone}, #{status}, #{sex}, #{birthday},
        #{signature}, #{location}, #{address}, #{remark});
    </insert>

    <update id="update">
        update base_user
        set gmt_modify = sysdate(),
        <trim prefix="set" prefixOverrides=",">
            <if test="modifier != null">
                modifier = #{modifier},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted},
            </if>
            <if test="userName != null">
                user_name = #{userName},
            </if>
            <if test="nickName != null">
                nick_name = #{nickName},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="birthday != null">
                birthday = #{birthday},
            </if>
            <if test="signature != null">
                signature = #{signature},
            </if>
            <if test="location != null">
                location = #{location},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
        </trim>
        where id = #{id}
    </update>

    <select id="queryByUid" resultType="cc.caker.mall.dal.base.po.BaseUserDO">
        select
        <include refid="BASE_COLUMN"/>
        from base_user
        where is_deleted = 'N'
        and uid = #{uid}
    </select>

</mapper>
